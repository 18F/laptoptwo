---

# Applications to be installed via Homebrew
# e.g. "brew install <thing>"
homebrew:
  - cf-cli
  - curl
  - git
  - github/gh/gh
  - hub
  - libgit2
  - the_silver_searcher
  - vim
  - wget

# Taps to install
# e.g. "brew tap <thing>"
taps:
  - cloudfoundry/homebrew-tap
  - homebrew/services

# Casks (Mac GUI apps) to install
# e.g. "brew cask <thing>"
casks:
  - chromedriver
  - docker
  - keepassxc
  - slack
